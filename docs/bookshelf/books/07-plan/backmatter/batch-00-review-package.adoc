[#batch-00-review-package]
== Batch 00 审核包

=== Batch

[horizontal]
批次编号:: `Batch 00`
批次名称:: `基线与 RDF 1.2 spike 归档`

=== Commit

[horizontal]
Batch 00 基线提交 hash:: `95ed433f6dda0b126660974b4b7a08027af26dfd`
提交信息:: `000`
状态与证据补交:: 本审核包所在提交；具体 hash 由执行者最终报告给出。

=== Scope

本批完成对象：

* RDF 1.2 triple term 与 `rdf:reifies` spike 证据归档。
* N3 作为 Turtle 1.2 serializer 与 parse-back test backend 的依赖事实。
* `@types/n3` RDF 1.2 triple term 类型表面风险。
* `rdf12` 作为唯一新增 CLI public format 和 TypeScript public entry 的执行契约。

本批未处理对象：

* 正式 RDF projection graph primitives。
* `src/rdf12-projection/` 正式实现。
* CLI `--format rdf12` 接入。
* Batch 01 及后续实现对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`docs/bookshelf/books/07-plan/`
|记录 RDF 1.2 行级投影器实施计划、Batch 00 spike 结论、public surface 边界、状态台账和审核包。

|`spike/`
|保留 N3 RDF 1.2 triple term、edge case 与 negative parser probe。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/book.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/04-rdf-coordinate-system.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/10-xref-occurrence-and-reification.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `package.json`
* `src/index.ts`
* `src/cli.ts`
* `spike/package.json`
* `spike/n3-rdf12-reifier-probe.ts`
* `spike/n3-rdf12-edge-probe.ts`
* `spike/n3-rdf12-parser-negative-probe.ts`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm probe:n3`
|PASS
|N3 写出 `rdf:reifies <<(...)>>`，parse-back 后 `rdf:reifies` object 为 `Quad`，并可 roundtrip。

|`pnpm probe:n3:edges`
|PASS
|N3 Store 对重复 asserted relation triple 去重，同时保留两个 occurrence reifier、candidate node 和 `xsd:integer` literal。

|`pnpm probe:n3:negative`
|PASS
|parser 将真实 triple term 解析为 `Quad`，将外观相似的 string literal 解析为 `Literal`。

|`pnpm exec tsc --noEmit -p tsconfig.json`
|PASS
|spike TypeScript strict typecheck 通过。
|===

=== Contract Checklist

* [x] 本批没有越过文件边界。
* [x] 本批没有修改与批次无关的文件。
* [x] 本批没有引入全局打回条件。
* [x] 本批新增 public surface 与执行契约一致：唯一新增 CLI format 与 TypeScript public entry 均为 `rdf12`。
* [x] 本批测试覆盖章节验收标准。
* [x] 本批没有把 N3 Store 定为 public graph contract。
* [x] 本批没有暴露 `rdf`、`ttl` 或 `turtle` public format。
* [x] 本批没有实现正式 RDF 投影器。

=== Executor Conclusion

[horizontal]
执行者结论:: `needs-review`
审核边界:: 本审核包只提交 Batch 00 证据，不宣布通过，不解锁 Batch 01。
