[#batch-10-review-package]
== Batch 10 审核包

=== Batch

[horizontal]
批次编号:: `Batch 10`
批次名称:: `查询契约验收`

=== Commit

[horizontal]
实现提交 hash:: `3d923ee99075f9f333c59968ddffd8f4af02cb4f`
修复提交 hash:: `e872aa44d95aae00d28730a5195f2ebd9365582a`
提交信息:: `test: add rdf12 query contract acceptance`；`test: tighten rdf12 query acceptance`

=== Scope

本批完成对象：

* `samples/reference-links.adoc` query contract acceptance。
* interdocument xref acceptance。
* payload query contract acceptance。
* public API 与 CLI RDF 1.2 parse-back acceptance。
* file + line reverse lookup acceptance。
* relation reifier triple term semantic acceptance。

本批未处理对象：

* 新投影语义。
* `src/rdf12-projection/` behavior change。
* Batch 11 final gates。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`test/rdf12-projection/reference-links.acceptance.test.ts`
|覆盖 label lookup、direct containment、file+line reverse lookup、xref occurrence、outgoing xref、relation reifier 和 surface attribute query contracts。

|`test/rdf12-projection/interdocument.acceptance.test.ts`
|覆盖 interdocument selector、official href evidence、无 local target node 和无 reifier 的 external xref behavior。

|`test/rdf12-projection/payload.acceptance.test.ts`
|覆盖 payload full/content span、opaque source text、node/xref payload binding 和 unbound payload selector。

|`test/rdf12-projection/query-contracts.test.ts`
|覆盖 public `rdf12` result 和 CLI `--format rdf12` stdout 的 parse-back graph facts。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/14-query-contracts.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/16-worked-example.adoc`

代码与 fixture：

* `src/rdf12-projection/`
* `samples/reference-links.adoc`
* `samples/interdocument-xref.adoc`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/reference-links.acceptance.test.ts test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/payload.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts`
|PASS
|33 个 test files、190 个 tests 通过；包含 Batch 10 acceptance tests。

|`pnpm test`
|PASS
|33 个 test files、190 个 tests 全量通过。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 132 个文件，无需修复。

|`git diff -- src/rdf12-projection`
|PASS
|投影 behavior files 无差异。

|`git diff --check 9ebf56c..HEAD`
|PASS
|Batch 10 implementation 与修复提交无 whitespace errors。
|===

=== Contract Checklist

* [x] AddressLabel query resource 已覆盖。
* [x] TitleLabel query section 已覆盖。
* [x] Section direct block query 已覆盖。
* [x] Xref occurrence query 已覆盖。
* [x] Section outgoing xref query 已覆盖，包含 `aat:sourceNode` 和 `aat:targetNode` graph facts。
* [x] Unbound/external selector query 已覆盖。
* [x] Relation reifier query 已覆盖，且检查 triple term semantics。
* [x] Surface attribute query 已覆盖。
* [x] Payload query 已覆盖。
* [x] File + line reverse lookup query 已覆盖，且按 `aat:relativePath` 与 line span 同时过滤。
* [x] CLI `rdf12` output parse-back acceptance 已覆盖。
* [x] Acceptance tests 使用 graph facts，不依赖 Turtle formatting。
* [x] Payload raw opacity 已覆盖。
* [x] Cross-document xref behavior 已覆盖。
* [x] 本批未修改 `src/rdf12-projection/` behavior files。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 02:50:55 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 Batch 10 test diff、query contract coverage、graph-level assertion style、external selector behavior、payload opacity 和 file+line reverse lookup；审核者打回一次以补齐 `sourceNode`/`targetNode` 与 relativePath lookup filter，复核修复提交后独立运行 focused acceptance tests、full tests、typecheck、lint、禁止 source diff 和 whitespace check，均为 PASS。

=== Reviewer Notes

* `test/fixtures/rdf12-payload-policy.adoc` 当前不存在；本批 payload acceptance 使用构造的 in-test `AbundantDocument`。审核接受该测试范围，因为 Batch 10 文件边界允许 acceptance tests，且未修改投影 behavior。
