[#batch-02-review-package]
== Batch 02 审核包

=== Batch

[horizontal]
批次编号:: `Batch 02`
批次名称:: `identity、path 与 projection options`

=== Commit

[horizontal]
提交 hash:: `e181e79ba7e1929774e0ec687bf6f61f0527afb1`
提交信息:: `feat: add rdf12 identity coordinates`

=== Scope

本批完成对象：

* `rdf12` projection options 与 normalization。
* `relativePath`、document root containment 和 `documentKey = sha256(relativePath)`。
* source digest normalization。
* resource IRI、local id helper 和 ordinal allocator。

本批未处理对象：

* provenance graph。
* structure node projection。
* CLI `rdf12` 接入。
* Batch 03 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`src/rdf12-projection/options.ts`
|定义 `Rdf12Options`、normalized options 和默认 `baseIri`。

|`src/rdf12-projection/path-coordinate.ts`
|计算 document root、source path、slash-normalized relative path 和 document key。

|`src/rdf12-projection/source-digest.ts`
|从显式 digest 或调用方提供的 source text 生成 normalized source digest。

|`src/rdf12-projection/resource-identity.ts`
|生成 resource IRI、block/occurrence/local helper 和 ordinal。

|`test/rdf12-projection/path-coordinate.test.ts`
|覆盖 document key、Windows separator、document root containment。

|`test/rdf12-projection/resource-identity.test.ts`
|覆盖 options、digest、IRI、local id 和 ordinal 行为。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/05-provenance-and-line-location.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/06-resource-identity.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/13-projection-algorithm.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `src/model.ts`
* `src/parser.ts`
* `src/source-lines.ts`
* `src/rdf12-projection/`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/path-coordinate.test.ts test/rdf12-projection/resource-identity.test.ts`
|PASS
|15 个 test files、120 个 tests 通过；包含 Batch 02 identity/path tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 97 个文件，无需修复。

|`git diff -- src/cli.ts src/parser.ts`
|PASS
|禁止路径无差异。
|===

=== Contract Checklist

* [x] `documentRoot` 为 required option。
* [x] `baseIri` 缺省值为 `urn:aat:doc:`。
* [x] relative path 以 `documentRoot` 为基准并使用 `/`。
* [x] relative path containment 使用 path API 判断，不用字符串前缀判断。
* [x] source path 越出 document root 时抛错。
* [x] `documentKey` 是 `sha256(relativePath)`。
* [x] `sourceDigest` 缺省时不生成占位符。
* [x] source digest 不进入 resource IRI。
* [x] 作者 ID 和标题文本不进入 resource IRI。
* [x] 本批不读取源文件内容，除非调用方提供 `sourceText`。
* [x] 本批没有创建 provenance graph、structure projection 或 CLI change。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 00:50:23 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 Batch 02 diff、06 号书 identity/path/source digest 章节和禁止路径，独立运行 Batch 02 tests、typecheck 和 lint，均为 PASS。
