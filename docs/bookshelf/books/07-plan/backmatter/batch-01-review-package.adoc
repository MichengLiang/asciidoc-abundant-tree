[#batch-01-review-package]
== Batch 01 审核包

=== Batch

[horizontal]
批次编号:: `Batch 01`
批次名称:: `RDF 1.2 graph primitives`

=== Commit

[horizontal]
实现提交 hash:: `d1580a25189e70f8714b870add9073b269d29c6f`
修复提交 hash:: `d3bd13d1ee8be1c9b7c991a5beace5e64d2f63dc`
提交信息:: `feat: add rdf12 graph primitives`；`fix: stabilize rdf12 graph ordering and lint spikes`

=== Scope

本批完成对象：

* 项目自有 RDF 1.2 IRI、literal、triple term 和 triple 类型。
* Graph add、has、match、dedupe、deterministic key 和 stable ordering。
* string、integer、boolean literal helpers。
* Triple term 与外观相似 string literal 的 key 区分测试。

本批未处理对象：

* `AbundantDocument` traversal。
* provenance graph。
* Turtle serialization。
* CLI 或 public TypeScript entry。
* Batch 02 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`src/rdf12-projection/namespaces.ts`
|定义 `aat`、`rel`、`rdf`、`rdfs`、`prov`、`dcterms`、`xsd` namespace constants。

|`src/rdf12-projection/terms.ts`
|定义 RDF 1.2 term、triple、triple term 与 recursive deterministic key。

|`src/rdf12-projection/literals.ts`
|定义 string、integer、boolean literal helpers。

|`src/rdf12-projection/graph.ts`
|定义项目自有 graph set、canonical-key dedupe、pattern query 与 code-unit stable ordering。

|`test/rdf12-projection/graph.test.ts`
|覆盖 Batch 01 graph primitive 验收项和 code-unit ordering 回归。

|`spike/n3-rdf12-*.ts`
|只做 Biome import ordering 和 formatting 修复，保持 Batch 00 probe 语义。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/04-rdf-coordinate-system.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/10-xref-occurrence-and-reification.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `spike/n3-rdf12-reifier-probe.ts`
* `spike/n3-rdf12-edge-probe.ts`
* `spike/n3-rdf12-parser-negative-probe.ts`
* `src/model.ts`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/graph.test.ts`
|PASS
|13 个 test files、108 个 tests 通过；包含 Batch 01 graph primitive tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 91 个文件，无需修复。

|`pnpm probe:n3`
|PASS
|N3 RDF 1.2 reifier triple term 写出、parse-back 与 roundtrip 仍通过。

|`pnpm probe:n3:edges`
|PASS
|N3 Store 去重、multiple reifiers、candidate node 和 integer literal probe 仍通过。

|`pnpm probe:n3:negative`
|PASS
|真实 triple term 与外观相似 string literal 仍可区分。

|`pnpm exec tsc --noEmit -p tsconfig.json`
|PASS
|`spike/` TypeScript typecheck 通过。
|===

=== Contract Checklist

* [x] 本批没有越过文件边界。
* [x] 本批没有修改 `src/parser.ts`、`src/cli.ts` 或 `src/index.ts`。
* [x] 本批没有读取或遍历 `AbundantDocument`。
* [x] 本批没有创建 provenance graph。
* [x] 本批没有输出 Turtle。
* [x] 本批没有暴露 N3 Store 作为 graph contract。
* [x] triple term 是结构对象，不是 string literal。
* [x] graph dedupe 基于 canonical key，不基于对象引用地址。
* [x] stable ordering 使用显式 code-unit comparator，不依赖 locale collation。
* [x] string literal `<<(s p o)>>` 不等于 triple term。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 00:32:00 +0800`
打回项:: `none`

审核证据:: 审核者独立检查实现与修复 diff，独立运行 Batch 01 graph test、typecheck、lint、Batch 00 spike probes 和 spike typecheck，均为 PASS；审核者确认禁止路径无差异。
