[#batch-08-review-package]
== Batch 08 审核包

=== Batch

[horizontal]
批次编号:: `Batch 08`
批次名称:: `RDF 1.2 serialization 与 roundtrip`

=== Commit

[horizontal]
提交 hash:: `f76bcb90f37a91f5b15620753584a5716d63bbbc`
提交信息:: `feat: serialize rdf12 projections as turtle`

=== Scope

本批完成对象：

* N3 runtime serializer/parser adapter。
* project graph 到 N3 quad 的转换。
* Turtle 1.2 serializer `serializeRdf12ProjectionToTurtle(projection)`。
* 测试用 Turtle parse-back 到 project graph 的转换。
* RDF graph semantic comparison helper。
* deterministic graph ordering 在 serialization 输入侧复用 project graph key order。
* RDF 1.2 triple term roundtrip 验收。

本批未处理对象：

* public `rdf12` TypeScript API。
* CLI `--format rdf12`。
* `src/index.ts` export surface。
* Batch 09 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`package.json`
|添加 `n3` runtime dependency 和 `@types/n3` dev dependency。

|`pnpm-lock.yaml`
|锁定 Batch 08 新增依赖。

|`src/rdf12-projection/n3-adapter.ts`
|封装 project RDF terms 与 N3 terms/quads 的互转，并提供测试用 Turtle parse-back helper。

|`src/rdf12-projection/turtle-serializer.ts`
|实现 `serializeRdf12ProjectionToTurtle(projection)`，使用 N3 Writer 输出 Turtle 1.2。

|`src/rdf12-projection/graph-canonicalization.ts`
|提供 graph key canonicalization、semantic equivalence 和 assertion helper。

|`test/rdf12-projection/turtle-serializer.test.ts`
|覆盖 prefix、IRI、literal、integer 和 `rdf:reifies <<( ... )>>` serialization，以及重复 serialization 稳定性。

|`test/rdf12-projection/roundtrip.test.ts`
|覆盖 parse-back semantic equivalence、triple term reifier、fake string literal negative case、integer、中文、多行、引号和反斜杠 literal roundtrip。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/04-rdf-coordinate-system.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/10-xref-occurrence-and-reification.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码与研究文件：

* `src/rdf12-projection/graph.ts`
* `src/rdf12-projection/terms.ts`
* `spike/n3-rdf12-reifier-probe.ts`
* `spike/n3-rdf12-edge-probe.ts`
* `spike/n3-rdf12-parser-negative-probe.ts`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/turtle-serializer.test.ts test/rdf12-projection/roundtrip.test.ts`
|PASS
|28 个 test files、175 个 tests 通过；包含 Turtle serializer 和 roundtrip tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 127 个文件，无需修复。

|`git diff -- src/cli.ts src/index.ts`
|PASS
|禁止 public CLI/API 路径无差异。

|`git diff --check a96d8fb..HEAD`
|PASS
|Batch 08 implementation commit 无 whitespace errors。

|full reference projection probe
|PASS
|`samples/reference-links.adoc` 投影 438 triples；Turtle 输出重复 serialization 稳定；parse-back graph semantic equivalence 成立；8 个 `rdf:reifies` object 全部解析为 N3 `Quad`。

|N3 boundary search
|PASS
|`src` 中 N3 import 只出现在 `n3-adapter.ts` 和 `turtle-serializer.ts`；`test` 只通过 adapter 读取 N3 boundary evidence。
|===

=== Contract Checklist

* [x] `n3` 是 runtime dependency。
* [x] `@types/n3` 是 dev dependency。
* [x] `serializeRdf12ProjectionToTurtle(projection)` 已实现。
* [x] Turtle output 包含 prefix declarations。
* [x] Turtle output 支持 IRI terms、plain string literals、xsd integer literals 和 triple terms。
* [x] `rdf:reifies` 使用 Turtle 1.2 triple term，而不是 string literal。
* [x] Turtle output 可以由 `format: "text/turtle"` 的 N3 parser parse back。
* [x] Parse-back graph 与 project graph 按 semantic keys 等价。
* [x] Fake string literal reifier 不满足 triple term assertion。
* [x] 中文、多行、引号与反斜杠 literal roundtrip 已覆盖。
* [x] Same graph repeated serialization 输出稳定。
* [x] Project public graph type 保持项目自有。
* [x] N3 Store 没有进入 `src/index.ts` exported contract。
* [x] `src/cli.ts` 与 `src/index.ts` 未修改。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 02:25:53 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 Batch 08 diff、dependency placement、N3 adapter boundary、serializer implementation、roundtrip tests 和 public path boundary；审核者独立运行 Batch 08 tests、typecheck、lint、禁止路径检查、whitespace check、N3 boundary search 和 full reference projection roundtrip probe，均为 PASS。

=== Reviewer Notes

* `turtle-serializer.ts` 当前依赖 N3 Writer callback 在本用法下同步完成；审核者用独立探针确认当前 N3 runtime 的 `Writer.end` callback 为同步触发。若后续替换 serializer backend，应重新审查该同步封装。
* `graph-canonicalization.ts` 使用 project graph 的 canonical key order 比较 graph 语义；该 helper 适合当前 deterministic project graph，不以 Turtle 排版作为验收依据。
