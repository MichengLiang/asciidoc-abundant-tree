[#batch-04-review-package]
== Batch 04 审核包

=== Batch

[horizontal]
批次编号:: `Batch 04`
批次名称:: `结构资源与 containment`

=== Commit

[horizontal]
提交 hash:: `e587a7ef3cdd6355124b8b67e59d6d02648803a4`
提交信息:: `feat: project rdf12 structural resources`

=== Scope

本批完成对象：

* `AbundantDocument.children` 到 structure resources 的投影。
* source location triples。
* direct `aat:containsDirectly` containment。
* 后续 label/xref 批次可复用的 node index。

本批未处理对象：

* label catalog。
* xref occurrence resource、xref relation triple 和 reifier。
* payload binding。
* Batch 05 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`src/rdf12-projection/node-index.ts`
|记录 input node 到 generated resource IRI 的映射。

|`src/rdf12-projection/source-location.ts`
|写入 `aat:relativePath`、line span 和 column triples。

|`src/rdf12-projection/structure-projector.ts`
|投影 section、paragraph、listing、table、anchor target 和 direct containment。

|`src/rdf12-projection/projector.ts`
|将 structure projection 接入 internal projector，并返回 node index。

|`test/rdf12-projection/structure-projector.test.ts`
|覆盖结构资源、source location、listing spans 和无 source scan 行为。

|`test/rdf12-projection/containment.test.ts`
|覆盖 direct containment、无 transitive closure 和 xref 未提前投影。

|`test/rdf12-projection/projector.test.ts`
|维护 Batch 03 旧断言，使其在 Batch 04 后只继续禁止 label resource。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/03-input-contract.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/08-structural-resources.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/09-containment-projection.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `src/model.ts`
* `src/official-projector.ts`
* `src/source-surfaces.ts`
* `src/source-interval-resolver.ts`
* `src/source-lines.ts`
* `samples/reference-links.adoc`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/structure-projector.test.ts test/rdf12-projection/containment.test.ts`
|PASS
|19 个 test files、132 个 tests 通过；包含 Batch 04 structure/containment tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 107 个文件，无需修复。

|`git diff -- src/official-projector.ts src/source-surfaces.ts src/parser.ts`
|PASS
|禁止路径无差异。
|===

=== Contract Checklist

* [x] structure resources 来自 `AbundantDocument.children`。
* [x] 本批没有按 section level 或 line range 重建 tree。
* [x] 可回源 structure resources 带 `aat:relativePath`、`aat:startLine`、`aat:endLine`。
* [x] paragraph resource 输出 `aat:sourceText`。
* [x] listing full span、metadata span、content span 分离。
* [x] containment 只输出 direct `children` 关系。
* [x] 不输出 containment transitive closure。
* [x] 本批没有 source scanner 或 parser 变更。
* [x] 本批没有生成 label catalog。
* [x] 本批没有生成 xref occurrence resource、relation triple 或 reifier。
* [x] 本批没有生成 payload binding。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 01:08:50 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 structure projector、node index、source location、projector boundary 和 Batch 03 test maintenance diff，独立运行 Batch 04 tests、typecheck、lint 和禁止路径检查，均为 PASS。
