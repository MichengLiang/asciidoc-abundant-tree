[#batch-05-review-package]
== Batch 05 审核包

=== Batch

[horizontal]
批次编号:: `Batch 05`
批次名称:: `label catalog 与 selector binding`

=== Commit

[horizontal]
实现提交 hash:: `140eb20c7fe8daffaf0aac813c07896900ebb981`
修复提交 hash:: `fcf922d2793a71618df3c5a6ff6497cba0c4c16e`；`9fc360a429aa654c3d96b8f521a15bdba9752f03`
提交信息:: `feat: add rdf12 label catalog`；`fix: project rdf12 xref display labels`；`fix: defer rdf12 xref display label ownership`

=== Scope

本批完成对象：

* Label resource projection。
* Label catalog。
* Selector binding result。
* `TargetNode` catalog supplementation。
* `XrefDisplayLabel` explicit-owner helper for Batch 06 reuse。

本批未处理对象：

* xref occurrence resource。
* xref relation triple 和 RDF 1.2 reifier。
* payload binding。
* Batch 06 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`src/rdf12-projection/labels.ts`
|投影 label resources，补充 `TargetNode` catalog entries，并提供 explicit-owner `XrefDisplayLabel` helper。

|`src/rdf12-projection/label-catalog.ts`
|维护 label value 到 owner resource 的 catalog，支持重复值。

|`src/rdf12-projection/selector-binding.ts`
|实现 unique、unresolved 和 ambiguous selector binding result。

|`src/rdf12-projection/node-index.ts`
|为 `TargetNode` supplementation 增加 target lookup metadata。

|`src/rdf12-projection/structure-projector.ts`
|向 node index 写入 target lookup 所需的 end line 与 target type。

|`src/rdf12-projection/projector.ts`
|将 label projection 接入 internal projector 并返回 label catalog。

|`test/rdf12-projection/labels.test.ts`
|覆盖 label classes、source location、role label、TargetNode 非 resource、XrefDisplayLabel owner 边界。

|`test/rdf12-projection/selector-binding.test.ts`
|覆盖 unique、unresolved、ambiguous 和 TargetNode supplementation binding。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/07-label-and-selector-model.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/08-structural-resources.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/03-input-contract.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `src/model.ts`
* `src/metadata-parser.ts`
* `src/binding-merge.ts`
* `test/official-projector.test.ts`
* `src/rdf12-projection/`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/labels.test.ts test/rdf12-projection/selector-binding.test.ts`
|PASS
|21 个 test files、145 个 tests 通过；包含 Batch 05 label/selector tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 112 个文件，无需修复。

|`git diff -- src/metadata-parser.ts src/binding-merge.ts src/parser.ts src/inline-occurrence-scanner.ts`
|PASS
|禁止路径无差异。
|===

=== Contract Checklist

* [x] label value 不进入 label resource IRI 或 structure resource IRI。
* [x] label resource 写入 `rdf:value`。
* [x] 可回源 label resource 写入 relative path 和 line span。
* [x] owner resource 通过 `aat:hasLabel` 指向 label resource。
* [x] label catalog 支持重复 value 和多个 owner。
* [x] unique selector 绑定到唯一 owner。
* [x] unresolved selector 保留 selector literal，不生成 target。
* [x] ambiguous selector 返回 candidates，不生成 single target。
* [x] `TargetNode` 只补充 catalog，不成为 projection resource。
* [x] `XrefDisplayLabel` 不在缺少合法 xref owner 时挂到 document/source/target node。
* [x] 本批没有生成 xref relation triple、RDF reifier 或 payload binding。
* [x] 本批没有修改 parser、metadata parser 或 binding merge。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 01:29:40 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 label identity、catalog ambiguity、TargetNode supplementation 和两次 XrefDisplayLabel 修复；审核者独立运行 Batch 05 tests、typecheck、lint 和禁止路径检查，均为 PASS。
