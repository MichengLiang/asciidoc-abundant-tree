[#batch-03-review-package]
== Batch 03 审核包

=== Batch

[horizontal]
批次编号:: `Batch 03`
批次名称:: `provenance 与 document resource`

=== Commit

[horizontal]
提交 hash:: `bc0133455b6205d03a1ab5286f6f293ce58ce1d0`
提交信息:: `feat: project rdf12 provenance resources`

=== Scope

本批完成对象：

* Internal `projectAbundantDocumentToRdf12` projector entry。
* `Rdf12Projection` object。
* Source document entity、projection entity、projection activity 和 AbundantDocument entity。
* Document resource。

本批未处理对象：

* section、paragraph、listing、table、xref、attribute 或 payload projection。
* structure child resource。
* label resource。
* CLI 或 public `src/index.ts` entry。
* Batch 04 及后续对象。

=== Files Changed

[cols="1,3", options="header"]
|===
|路径 |作用

|`src/rdf12-projection/projector.ts`
|创建 internal projector entry 和 `Rdf12Projection` 返回对象。

|`src/rdf12-projection/provenance.ts`
|写入 Batch 03 required provenance triples。

|`src/rdf12-projection/document-resource.ts`
|写入 document resource type 和 relative path triples。

|`test/rdf12-projection/provenance.test.ts`
|覆盖 provenance required triples 和 source digest 行为。

|`test/rdf12-projection/projector.test.ts`
|覆盖空 document projection、source/document IRI 分离和 Batch 04 对象未生成。
|===

=== Required Context Read

06 号书原文：

* `docs/bookshelf/books/06-rdf12-line-projection/chapters/05-provenance-and-line-location.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/08-structural-resources.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/13-projection-algorithm.adoc`
* `docs/bookshelf/books/06-rdf12-line-projection/chapters/15-invariants-and-acceptance.adoc`

代码入口：

* `src/model.ts`
* `src/parser.ts`
* `src/rdf12-projection/`

=== Acceptance Evidence

[cols="2,1,3", options="header"]
|===
|命令 |结果 |摘要

|`pnpm test -- test/rdf12-projection/provenance.test.ts test/rdf12-projection/projector.test.ts`
|PASS
|17 个 test files、125 个 tests 通过；包含 Batch 03 provenance/projector tests。

|`pnpm typecheck`
|PASS
|`tsc --noEmit` 通过。

|`pnpm lint`
|PASS
|Biome 检查 102 个文件，无需修复。

|`git diff -- src/cli.ts src/index.ts src/parser.ts`
|PASS
|禁止路径无差异。
|===

=== Contract Checklist

* [x] Internal projector function 已创建，未从 `src/index.ts` 暴露。
* [x] `Rdf12Projection` 至少包含 graph、prefixes、relativePath、documentIri、sourceDocumentIri、projectionIri。
* [x] source document entity 与 document resource 使用不同 IRI。
* [x] provenance required triples 齐全。
* [x] `sourceDigest` 缺失时 graph 不含 `aat:sourceDigest`。
* [x] `sourceDigest` 存在时 graph 含 `aat:sourceDigest`。
* [x] document resource 含 `rdf:type aat:AsciiDocDocument` 与 `aat:relativePath`。
* [x] 本批不生成 structure child resource。
* [x] 本批不生成 label resource。
* [x] 本批没有修改 CLI、public index 或 parser。

=== Reviewer Decision

[horizontal]
审核结论:: `通过`
审核者:: `Codex principal coordinator`
审核时间:: `2026-05-28 00:56:51 +0800`
打回项:: `none`

审核证据:: 审核者独立检查 provenance triple list、projector boundary、source/document IRI 分离和禁止路径，独立运行 Batch 03 tests、typecheck 和 lint，均为 PASS。
