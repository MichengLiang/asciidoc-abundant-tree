[#batch-04-review-package]
== Batch 04 审核包：Xref 边与 Reifier

=== 批次信息

[horizontal]
批次编号:: `Batch 04`
批次名称:: `Xref 边与 Reifier`
提交 hash:: `0deadc878cd20f3c1ff08cb548a3e04172f9d63f`
执行者:: `Codex`
审核者:: `待定`
审核状态:: `待审核`

=== 修改文件

[source,text]
----
src/rdf12-projection/xref-projector.ts
src/rdf12-projection/projector.ts
src/rdf12-projection/resource-identity.ts
test/rdf12-projection/xref-projector.test.ts
test/rdf12-projection/reifier.test.ts
test/rdf12-projection/relation-predicate.test.ts
test/rdf12-projection/query-contracts.test.ts
test/rdf12-projection/heading-projection.acceptance.test.ts
docs/bookshelf/books/07-rdf12-heading-projection-implementation-plan/backmatter/batch-04-review-package.adoc
docs/bookshelf/books/07-rdf12-heading-projection-implementation-plan/backmatter/batch-status-ledger.adoc
----

=== 完成范围

[source,text]
----
- xref evidence resource type 从 aat:XrefOccurrence 迁移为 aat:XrefEdge。
- xref evidence local id 使用 xref-edge-l<line>-c<column>-o<ordinal>。
- xref edge evidence 使用 aat:sourceHeading、aat:targetHeading 和 aat:candidateHeading。
- xref rel control field 使用 aat:rel 保存原始 rel 字符串。
- xref display text 使用 aat:displayLabel literal，不创建 label resource 或 catalog entry。
- xref edge evidence 保留 aat:targetSelector、aat:raw、source span、syntax、official href/resolution evidence。
- source heading、target heading 和 predicate 均确定时生成主 relation triple。
- 主 relation triple 使用 rdf:reifies triple term 连接到 edge evidence。
- 缺省 rel 映射为 aat:references。
- token、CURIE 和 absolute IRI rel 继续通过 relation-predicate 语义映射。
- invalid rel 保留 aat:rel，不生成主 relation triple 或 reifier。
- unresolved target selector 保留 edge evidence 和 targetSelector，不生成 targetHeading 或主 relation triple。
- ambiguous target selector 输出全部 candidateHeading，不生成 targetHeading 或主 relation triple。
- relation triple subject/object 均为 heading node。
- source heading 由 xref.sourceSpan 所在 heading slice 决定，不通过 public selector binding 决定。
- duplicate source label 时，xref 仍绑定到实际包含该 xref 的 heading，并生成 relation triple 与 reifier。
- xref 位于 document title/preamble heading slice 且没有 containingSectionId 时，仍按 sourceSpan 所在 heading slice 绑定 sourceHeading，并生成 relation triple 与 reifier。
- containingSectionId 只控制 aat:sourceSelector evidence 是否写入，不阻断 sourceHeading slice resolution。
- heading-projection.acceptance.test.ts 中 Batch 04 xref edge/reifier 断言解除 expected-fail；Batch 05、Batch 06 和 Batch 07 仍保持 expected-fail。
----

=== 未处理对象

[source,text]
----
- kind/status/owner/weight 等 direct field predicates：归属 Batch 05。
- xref weight 作为 edge evidence direct predicate：归属 Batch 05。
- 节点 payload 和边 payload complex property binding：归属 Batch 06。
- payload projector、attributes projector 和旧 acceptance/query contract 全面清理：归属 Batch 05 到 Batch 07。
- public 契约最终清理和旧类型退出最终门禁：归属 Batch 07。
----

=== TDD 证据

[source,text]
----
RED:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts test/rdf12-projection/reifier.test.ts test/rdf12-projection/relation-predicate.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts
结果：FAIL。5 test files 中 3 failed，13 tests failed，失败点为缺少 aat:XrefEdge、xref-edge local id、sourceHeading/targetHeading/candidateHeading、aat:rel、aat:displayLabel、Batch 04 acceptance 和 query contract 的 outgoing edge evidence。

GREEN:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts test/rdf12-projection/reifier.test.ts test/rdf12-projection/relation-predicate.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts
结果：PASS。5 test files passed，30 passed tests，3 expected fail tests。

打回返工 RED:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts
结果：FAIL。新增 duplicate source label regression 后，xref physically inside 第二个 `dup` heading 时缺少 sourceHeading、relation triple 和 reifier。

打回返工 GREEN:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts test/rdf12-projection/reifier.test.ts test/rdf12-projection/relation-predicate.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts
结果：PASS。5 test files passed，31 passed tests，3 expected fail tests。

第二次打回返工 RED:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts
结果：FAIL。新增 document title/preamble xref regression 后，xref 没有 containingSectionId 但 sourceSpan 位于 document-title heading slice 内时缺少 sourceHeading、relation triple 和 reifier。

第二次打回返工 GREEN:
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts
结果：PASS。1 test file passed，14 passed tests。
----

=== 已运行命令

[source,bash]
----
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts test/rdf12-projection/reifier.test.ts test/rdf12-projection/relation-predicate.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts
pnpm typecheck
pnpm exec biome check --vcs-use-ignore-file=false src/rdf12-projection/xref-projector.ts test/rdf12-projection/xref-projector.test.ts
----

=== 命令结果

[source,text]
----
pnpm exec vitest run test/rdf12-projection/xref-projector.test.ts test/rdf12-projection/reifier.test.ts test/rdf12-projection/relation-predicate.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts: PASS, 5 test files passed, 32 passed tests, 3 expected fail tests.
pnpm typecheck: PASS.
pnpm exec biome check --vcs-use-ignore-file=false src/rdf12-projection/xref-projector.ts test/rdf12-projection/xref-projector.test.ts: PASS, Biome checked 2 files with no fixes applied.
----

=== 规约检查清单

* 本批没有修改 `src/inline-occurrence-scanner.ts`、`src/macro-argument-parser.ts` 或 `src/parser.ts`。
* 本批没有修改 payload projector 或 attributes projector。
* 本批 public graph 不输出 `aat:XrefOccurrence`。
* 本批 relation triple 的 subject 和 object 均为 heading node。
* 本批 `rdf:reifies` object 是 RDF 1.2 triple term。
* 本批 display label 不参与 selector binding 或 resource identity。
* 本批 source heading 不通过 public selector binding 决定。
* 本批 duplicate source label 时按 xref 所在 heading slice 选择实际 source heading。
* 本批 document title/preamble xref 即使没有 containingSectionId，也按 xref 所在 heading slice 选择 document-title source heading。
* 本批 sourceSelector evidence 只在 containingSectionId 存在时输出。
* 本批 unresolved target selector 不生成 targetHeading、relation triple 或 reifier。
* 本批 ambiguous target selector 输出 candidateHeading，且不生成 targetHeading、relation triple 或 reifier。
* 本批 invalid rel 保留 aat:rel，且不生成 relation triple 或 reifier。
* 本批没有实现 Batch 05 direct field predicates。
* 本批没有实现 Batch 06 payload binding。
* 本批测试覆盖了正向 xref relation edge/reifier 事实。
* 本批测试覆盖了必要负向事实。
* 本批 public graph 与 06 号书 Batch 04 对象语言一致。

=== 审核结论

[horizontal]
结论:: `待审核`
理由:: `执行者提交 Batch 04 第二次打回返工实现与测试，等待审核者确认 document title/preamble xref 的 source heading 由 xref 所在 heading slice 决定，且 target selector 的未绑定和多义路径仍没有主 relation triple。`
后续动作:: `审核通过后进入 Batch 05；若审核打回，则返工 Batch 04。`
