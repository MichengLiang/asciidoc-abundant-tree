[#batch-07-review-package]
== Batch 07 审核包：公共契约与最终门禁

=== 批次信息

[horizontal]
批次编号:: `Batch 07`
批次名称:: `公共契约与最终门禁`
提交 hash:: `49a7c602997f35aabcad493ecb792064ef599edd`
执行者:: `Codex`
审核者:: `Codex`
审核状态:: `通过`

=== 修改文件

[source,text]
----
README.md
CHANGELOG.md
test/rdf12-projection/heading-projection.acceptance.test.ts
test/rdf12-projection/interdocument.acceptance.test.ts
test/rdf12-projection/query-contracts.test.ts
test/rdf12-projection/roundtrip.test.ts
docs/bookshelf/books/07-rdf12-heading-projection-implementation-plan/backmatter/batch-07-review-package.adoc
docs/bookshelf/books/07-rdf12-heading-projection-implementation-plan/backmatter/batch-status-ledger.adoc
----

=== 完成范围

[source,text]
----
- 删除 heading-projection.acceptance.test.ts 中 Batch 00 迁移期 expected-fail wrapper。
- interdocument acceptance 从旧 aat:XrefOccurrence 查询面迁移到 aat:XrefEdge、aat:targetHeading 和 rdf:reifies 契约。
- query-contracts.test.ts 覆盖按 addressLabel/headline 查标题、读取 heading raw、直接子标题、递归子树、previousSibling、xref edge、edge payload、node payload、直接字段谓词。
- query-contracts.test.ts 增加最终负门禁：旧类型与旧谓词不出现在公共 RDF12 图中。
- roundtrip fixture 使用 heading/xref-edge/payload raw 语义，不再示范旧 label/sourceText fixture surface。
- README RDF12 描述更新为标题投影图公共对象：Heading、XrefEdge、direct field predicates、payload complex property objects。
- CHANGELOG 记录 RDF12 public graph 收敛为标题投影图，并移除旧 public contract surface。
----

=== 未处理对象

[source,text]
----
- 无。本批为 07 号计划最终公共契约门禁批次，后续状态由审核者决定。
----

=== TDD 证据

[source,text]
----
RED:
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/public-api.test.ts test/rdf12-projection/roundtrip.test.ts test/cli.test.ts
结果：FAIL。5 test files 中 interdocument.acceptance.test.ts 3 tests failed。失败点为测试仍按 aat:XrefOccurrence 旧类型过滤 xref 证据，导致找不到外部与本地 xref edges。

GREEN:
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/public-api.test.ts test/rdf12-projection/roundtrip.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts test/cli.test.ts
结果：PASS。6 test files passed，44 tests passed。
----

=== 已运行命令

[source,bash]
----
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts
pnpm exec vitest run test/rdf12-projection/query-contracts.test.ts
pnpm exec vitest run test/rdf12-projection/roundtrip.test.ts
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/public-api.test.ts test/rdf12-projection/roundtrip.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts test/cli.test.ts
pnpm exec vitest run test/rdf12-projection
pnpm test -- test/rdf12-projection
pnpm test -- test/cli.test.ts
pnpm typecheck
pnpm exec biome check --vcs-use-ignore-file=false test/rdf12-projection/heading-projection.acceptance.test.ts test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/roundtrip.test.ts
pnpm lint
pnpm build
pnpm pack:check
pnpm smoke:cli
pnpm --dir docs/bookshelf test
pnpm --dir docs/bookshelf run build
pnpm check
----

=== 命令结果

[source,text]
----
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts: PASS。1 test file passed，3 tests passed。
pnpm exec vitest run test/rdf12-projection/query-contracts.test.ts: PASS。1 test file passed，6 tests passed。
pnpm exec vitest run test/rdf12-projection/roundtrip.test.ts: PASS。1 test file passed，7 tests passed。
pnpm exec vitest run test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/public-api.test.ts test/rdf12-projection/roundtrip.test.ts test/rdf12-projection/heading-projection.acceptance.test.ts test/cli.test.ts: PASS。6 test files passed，44 tests passed。
pnpm exec vitest run test/rdf12-projection: PASS。25 test files passed，135 tests passed。
pnpm test -- test/rdf12-projection: PASS。package script 实际收集 38 test files；38 passed，251 tests passed。
pnpm test -- test/cli.test.ts: PASS。package script 实际收集 38 test files；38 passed，251 tests passed。
pnpm typecheck: PASS。
pnpm exec biome check --vcs-use-ignore-file=false test/rdf12-projection/heading-projection.acceptance.test.ts test/rdf12-projection/interdocument.acceptance.test.ts test/rdf12-projection/query-contracts.test.ts test/rdf12-projection/roundtrip.test.ts: PASS，Biome checked 4 files with no fixes applied。
pnpm lint: PASS。执行者首次运行因 ignored tmp/structural-payload-current.json 格式失败；该 tmp 文件按工作区规则为 disposable，已机械格式化且未纳入提交。审核者重跑 PASS，Biome checked 164 files with no fixes applied；聚合 pnpm check 中 lint PASS，Biome checked 166 files with no fixes applied。
pnpm build: PASS。
pnpm pack:check: PASS。publint all good；npm pack --dry-run 仅输出 npm config warnings。
pnpm smoke:cli: PASS。
pnpm --dir docs/bookshelf test: PASS。11 tests passed。
pnpm --dir docs/bookshelf run build: PASS。
pnpm check: PASS。lint、typecheck、test、pack:check、smoke:cli 全部通过；其中 test 收集 38 test files，251 tests passed。
----

=== 规约检查清单

* 本批没有修改 `src/parser.ts` 或 `src/asciidoctor-adapter.ts`。
* 本批没有新增 `rdf`、`ttl` 或 `turtle` CLI format alias。
* 本批保留 Batch 06 payload 语义：payload complex object 使用 `aat:payload`，不输出 public `PayloadBlock`、`hasPayload` 或 `payloadOf`。
* 本批删除迁移期 expected-fail wrapper，测试中无 `it.fails` 或 `migrationAllowedFailureCases`。
* 本批 query contract 覆盖 06 号书查询表面。
* 本批 final negative gate 覆盖旧 public RDF12 类型和旧 public RDF12 谓词。
* Turtle roundtrip gate 保留 rdf:reifies triple term 结构。
* JSON-LD public API/CLI gate 保留 rdf12:TripleTerm 结构。
* README 和 CHANGELOG 不再把旧结构资源、label resources、surface attributes 或 payload block resources 描述为当前 RDF12 public graph。

=== 审核结论

[horizontal]
结论:: `通过`
理由:: `已从 public API、CLI、Turtle roundtrip、JSON-LD shape 和 graph/query contract tests 五个表面确认同一标题投影对象；迁移期 expected-fail wrapper 已删除；interdocument xref 查询面已迁移到 XrefEdge；最终负门禁覆盖旧结构类型、label resource、SurfaceAttribute、PayloadBlock、XrefOccurrence 和旧谓词；README 与 CHANGELOG 不再把旧结构图描述为当前 RDF12 public graph。`
后续动作:: `07 号 RDF12 heading projection implementation plan 完成。`
