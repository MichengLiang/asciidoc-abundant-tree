name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10.33.0
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: docs/bookshelf/pnpm-lock.yaml
      - uses: actions/configure-pages@v6
      - name: Install bookshelf dependencies
        run: pnpm install --frozen-lockfile
        working-directory: docs/bookshelf
      - name: Test bookshelf scripts
        run: pnpm run test
        working-directory: docs/bookshelf
      - name: Build bookshelf
        run: pnpm run build
        working-directory: docs/bookshelf
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/bookshelf/build/html

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
